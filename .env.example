# Backend configuration
# Copy this file to .env and fill in the values as needed.

# Required: OpenRouter API key (or set API_KEY for fallback)
# Get one at https://openrouter.ai/
OPENROUTER_API_KEY=
# Optional: per-request client-side API key can override this
# API_KEY=

# Default model for OpenRouter (can be overridden from the frontend)
OPENROUTER_MODEL=deepseek/deepseek-chat-v3.1:free
# OPENROUTER_BASE_URL=https://openrouter.ai/api/v1

# Optional: Tavily for websearch (fallback to DuckDuckGo if empty)
TAVILY_API_KEY=

# Upload settings
# Max upload size in megabytes (per file)
MAX_UPLOAD_SIZE_MB=10
# Comma-separated list of allowed MIME types
ALLOWED_UPLOAD_TYPES=image/jpeg,image/png,image/gif,application/pdf,application/vnd.openxmlformats-officedocument.wordprocessingml.document,application/msword,text/plain
# Optional custom uploads directory
# UPLOAD_DIR=./uploads

# Session memory persistence
PERSIST_SESSIONS=true
# Optional custom path for session store
# SESSION_STORE_PATH=./session_store.json

# Prompt/history limits
MAX_TURNS=10
MAX_PROMPT_CHARS=6000

# Frontend deployment note
# In production, the frontend gets VITE_API_BASE_URL injected by the platform (see render.yaml).
# For local development, Vite proxy forwards /api to http://127.0.0.1:8001 per vite.config.ts.
